image: registry.gitlab.com/gitlab-org/gitlab-runner/gitlab-runner-helper

# before_script:
#   - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY

stages:
  - build
  # - deploy

build:
  stage: build
  script:
    - docker build --tag 3d-bot .
  only:
    - master

# deploy-prod:
#   stage: deploy
#   script:
#     - docker stack deploy -c docker-compose.yml --with-registry-auth my-stack
#   only:
#     - master
