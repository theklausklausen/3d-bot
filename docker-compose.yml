version: '3.5'
services:
  3d-bot:
      build:
        context: .
        dockerfile: ./Dockerfile.dev
      volumes:
        - ./:/app
      env_file:
        - ./workspace.env
      ports:
        - 3001:3001
      restart: 'unless-stopped'
      # healthcheck:
      #   test: ['CMD', 'rm', '/tmp/health-check.ok']
      #   interval: 30s
      #   timeout: 5s
      #   retries: 1
      #   start_period: 10s